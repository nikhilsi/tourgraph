# TourGraph Production Environment
# Copy to /opt/app/.env.production.local (Next.js auto-loads this in production)

# Viator Partner API (Basic tier, free affiliate)
VIATOR_API_KEY=your-production-key-here

# Anthropic Claude (only needed if running indexer on server — not used at request time)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Database (absolute path on server)
DATABASE_PATH=/opt/app/data/tourgraph.db

# Node environment
NODE_ENV=production

# Port (Next.js default, must match nginx upstream)
PORT=3000
