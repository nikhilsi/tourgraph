# Utilities

Database diagnostics and audit tools.

## Scripts

### `check-db.ts` — Database Audit

Quick stats: weight category distribution, tour count, one-liner coverage %, continent spread, top destinations.

```bash
npx tsx src/scripts/utils/check-db.ts
```

Run after any indexing operation to verify data quality.
